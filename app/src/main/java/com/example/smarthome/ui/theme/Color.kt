package com.example.smarthome.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ─────────────────────────────────────────────────────────────────────
val BrandBlue      = Color(0xFF4A90D9)
val BrandBlueDark  = Color(0xFF2D6FBF)

// ── Backgrounds ───────────────────────────────────────────────────────────────
val PageBackground = Color(0xFFF2F2F7)   // light grey like reference
val CardDark       = Color(0xFF2D6FBF)   // near-black card (ON state)
val CardLight      = Color(0xFFFFFFFF)   // white card (OFF state)
val BottomNavBg    = Color(0xFF2D6FBF)   // black bottom nav

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFF2D6FBF)
val TextSecondary  = Color(0xFF8E8E93)
val TextLight      = Color(0xFFFFFFFF)
val TextDimmed     = Color(0xFFAEAEB2)

// ── Semantic ──────────────────────────────────────────────────────────────────
val SuccessGreen   = Color(0xFF34C759)
val WarningAmber   = Color(0xFFFF9500)
val ErrorRed       = Color(0xFFFF3B30)
val RainBlue       = Color(0xFF007AFF)
val RainBlueBg     = Color(0xFFE8F1FF)

// ── Divider / Border ──────────────────────────────────────────────────────────
val DividerColor   = Color(0xFFE5E5EA)
val BorderLight    = Color(0xFFD1D1D6)